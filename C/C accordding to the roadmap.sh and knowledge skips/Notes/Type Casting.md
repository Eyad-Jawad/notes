in Type casting, division, there are rules:
`short` & `byte` **values** -> `int`
if there's `long`, expression -> `long`
if there's `float`, expression -> `float`
if there's `double`, result -> `double

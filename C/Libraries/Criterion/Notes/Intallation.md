I installed it by executing:

```Bash

sudo pacman -S criterion

```

there are other ways you can install it through, like copying the GitHub repo, or using some package manager, or in other oses like Ubuntu:

```Bash

sudo apt install libcriterion-dev

```

